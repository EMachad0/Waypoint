# Issues

The work queue. One directory per effort, numbered issue files inside it, deleted in the pull
request that implements them.

These files describe what is not true yet, so nothing here is a statement of current convention.
Conventions live in the docs one level up.

Conventions and wayfinding operations: `docs/agents/issue-tracker.md`. Status strings:
`docs/agents/triage-labels.md`.
